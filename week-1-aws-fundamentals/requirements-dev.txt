boto3>=1.34.0      # AWS SDK — needed for local testing
pytest>=8.0.0      # Testing framework
moto>=5.0.0        # AWS service mocking for tests
awscli>=1.30.0    # AWS CLI for local testing and deployment
aws-sam-cli>=1.100.0  # AWS SAM CLI for local development and testing